name: Deploy to GitHub Pages

on:
  push:
    branches: [ cursor-test ]
  pull_request:
    branches: [ cursor-test ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: github.ref == 'refs/heads/cursor-test'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: . 